DROP TABLE IF EXISTS MenuTb;

CREATE TABLE `MenuTb`
(

    `burger_name` VARCHAR (40),
    `patty` VARCHAR (40),
    `toppings` VARCHAR (120),
    `sauce` VARCHAR (40)
    `sidedish` VARCHAR (40)
    `drink` VARCHAR (40)

)
